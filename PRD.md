# 本地 KTV App 产品需求文档（PRD）

> 版本：v0.1（MVP 设计稿）
> 平台：Android
> 定位：本地优先的 KTV 播放器，导入 MP3 即可一键切换原唱/伴唱，零服务器运营成本

---

## 1. 产品概述

### 1.1 一句话描述
一款离线可用的 Android KTV App：导入普通 MP3，App 自动用 AI 分离人声/伴奏，K 歌时一根滑杆即可在原唱与伴奏之间任意切换，所有处理结果全部缓存在本地，下次播放秒开。

### 1.2 核心价值
- **零运营成本**：所有 AI 推理在端侧完成，不依赖服务器
- **零等待感**：流式分离 + 持久缓存，首次 2-3 秒预热，再次播放即开即唱
- **完全离线**：歌曲、双轨音频、歌词全部本地存储，无网也能用

### 1.3 不做的事情（MVP 范围外）
- 录音打分、混响、变调（V2 再考虑）
- 投屏、TV 端
- 社交分享、云同步
- 在线伴奏曲库（用户自己导入歌曲）

---

## 2. 用户与场景

| 用户 | 场景 | 核心诉求 |
|---|---|---|
| 个人 K 歌爱好者 | 在家用蓝牙音响练歌 | 本地 MP3 一键变伴奏 |
| 朋友聚会 | 没有 KTV 设备时手机外放 | 列表点歌、切歌、找歌快 |
| 学唱新歌 | 学习一首新歌的发音/旋律 | 单独听人声、降低伴奏 |

---

## 3. 功能清单

### 3.1 歌曲管理
- **导入方式**
  - 扫描设备 `Music/` 目录
  - 用户手动选 MP3 文件
  - 选择文件夹批量导入
  - 支持网络 URL 下载到本地后入库（粘贴 URL 即下载）
- **元数据解析**：从 ID3 tag 读取歌名、歌手、专辑、封面、时长
- **状态字段**：未分离 / 排队中 / 分离中（带进度）/ 已分离 / 失败

### 3.2 流式人声分离（核心能力）
- **触发时机**：用户首次播放该歌曲时
- **流程**：
  1. 预热：先分离前 ~10 秒（约 2-3s 完成）
  2. 开始播放，同时后台滚动分离剩余部分
  3. 整曲分离完成后，将 `vocals` 和 `accompaniment` 分别编码为 Opus 落盘
  4. 数据库标记 `separation_status = done`
- **下次播放**：直接读两个 Opus 文件，**不再触发 AI 推理**
- **中断续做**：分离过程中退出 App / 切歌，已分离的 chunk 持久化，下次接着分（按 chunk 编号续做）
- **任务调度**：使用 WorkManager 后台队列，可配置「仅在充电时分离队列中其他歌曲」

### 3.3 双轨播放器
- **同步播放**两路音频流（vocals + accompaniment）
- **控件**：
  - 主滑杆：人声音量 0%~100%（0 = 纯伴奏，100 = 原唱，中间可自由调节）
  - 总音量、播放/暂停、进度条、上一首/下一首
- **切换原伴唱无感延迟**（仅改音量增益，不重启播放器）
- **未分离的歌也能播**：直接播原 MP3，UI 上隐藏原伴唱滑杆并提示「正在准备伴奏…」

### 3.4 歌词
- **格式**：LRC（逐行）；预留 KRC（逐字）扩展
- **来源优先级**：
  1. 同名 `.lrc` 文件（如 `xxx.mp3` 旁的 `xxx.lrc`）
  2. MP3 ID3 tag 内嵌的 USLT/SYLT 帧
  3. 用户手动导入 `.lrc`
- **显示**：滚动显示，当前行高亮、字号可调
- 无歌词时显示「暂无歌词」，不阻断播放

### 3.5 搜索
- **匹配字段**：歌名、歌手、专辑
- **拼音支持**：中文歌名/歌手转拼音首字母索引（例如「邓紫棋」→ `dzq` 可命中）
- **响应**：本地 SQLite 索引，输入即搜，<100ms 返回
- **历史**：保留最近 10 条搜索记录

### 3.6 点歌队列
- **添加到队列**：从歌曲列表/搜索结果右侧「+」加入队列
- **队列管理**：
  - 拖动排序、删除、置顶（下一首）
  - 队列持久化（重启 App 队列还在）
- **播放完自动切下一首**

### 3.7 列表与分页
- **歌曲库列表**：支持按 `导入时间 / 歌名 / 歌手 / 最近播放 / 播放次数` 排序
- **分页**：每页 50 条，无限滚动加载（基于 Paging 3）
- **筛选 tab**：全部 / 已分离 / 未分离 / 收藏

### 3.8 收藏
- 心形图标一键收藏，单独 tab 查看

---

## 4. 数据模型

### 4.1 SQLite 表结构

**songs**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | TEXT PK | 文件内容 hash（去重用） |
| title | TEXT | 歌名 |
| artist | TEXT | 歌手 |
| album | TEXT | 专辑 |
| duration_ms | INT | 时长 |
| mp3_path | TEXT | 原文件路径 |
| vocals_path | TEXT | 人声轨路径（分离后） |
| accomp_path | TEXT | 伴奏轨路径（分离后） |
| lrc_path | TEXT | 歌词文件路径 |
| cover_path | TEXT | 封面图路径 |
| separation_status | TEXT | pending / running / done / failed |
| separation_progress | REAL | 0.0~1.0 |
| pinyin_initials | TEXT | 拼音首字母索引 |
| created_at | INT | 导入时间戳 |
| last_played_at | INT | 最近播放时间 |
| play_count | INT | 播放次数 |
| is_favorite | INT | 0/1 |

**queue**
| 字段 | 类型 |
|---|---|
| id | INT PK |
| song_id | TEXT FK |
| position | INT |
| added_at | INT |

**search_history**
| 字段 | 类型 |
|---|---|
| keyword | TEXT PK |
| searched_at | INT |

### 4.2 文件存储布局
```
/Android/data/<package>/files/
├── songs/
│   └── <song_id>/
│       ├── original.mp3        # 原始文件（或软链）
│       ├── vocals.opus         # 人声轨（96kbps Opus，~3MB/3min）
│       ├── accomp.opus         # 伴奏轨
│       ├── lyrics.lrc
│       └── cover.jpg
├── separation_chunks/          # 中断续做的临时 chunk
│   └── <song_id>/
│       ├── 000.pcm
│       └── 001.pcm
└── db/
    └── ktv.db
```

---

## 5. 关键流程

### 5.1 首次播放未分离的歌
```
用户点播放
  ↓
查 DB：status = pending
  ↓
启动流式分离 Worker（高优先级）
  ↓
分离前 10s（≈2-3s）
  ↓
开始播放（player 从已分离的 chunk 读双轨）
  ↓
后台继续分离剩余部分（每完成一个 chunk 写入 chunk 目录）
  ↓
全曲分离完成 → 合并为 vocals.opus + accomp.opus → 删 chunk → status = done
```

### 5.2 已分离的歌再次播放
```
查 DB：status = done
  ↓
读 vocals.opus + accomp.opus
  ↓
ExoPlayer 双 source 同步播放（< 500ms 启动）
```

### 5.3 跳进度条到未分离区段
```
检查目标位置 chunk 是否存在
  ├─ 存在 → 直接 seek
  └─ 不存在 → 暂停 1-2s，优先分离目标 chunk → seek
```

### 5.4 切原伴唱
```
滑杆事件 → 改 vocals 通道 gain（0~1 线性映射）→ 0ms 生效
```

---

## 6. 技术架构

| 层 | 选型 | 备注 |
|---|---|---|
| 语言 | Kotlin | |
| UI | Jetpack Compose | |
| 数据库 | Room (SQLite) | |
| 分页 | Paging 3 | |
| 后台任务 | WorkManager | 分离队列 |
| 异步 | Coroutines + Flow | |
| 播放器 | ExoPlayer + Oboe | 双 source 同步 + 低延迟混音 |
| AI 推理 | TFLite (NNAPI / GPU delegate) | Spleeter 2-stems |
| 音频编码 | libopus（NDK） | 双轨持久化 |
| 拼音 | pinyin4j 或 TinyPinyin | 中文搜索 |
| 歌词解析 | 自写 LRC parser | 简单稳定 |

### 6.1 模块划分
```
:app                 // UI + 入口
:player              // 双轨同步播放器（ExoPlayer + Oboe 混音）
:separator           // 流式分离引擎（TFLite + chunk 管理）
:data                // Room + Repository
:lyrics              // LRC 解析与同步
:importer            // 文件扫描、元数据提取、URL 下载
:common              // 工具类（拼音、文件、hash）
```

---

## 7. 性能与质量目标

| 指标 | 目标 |
|---|---|
| 已分离歌曲冷启动到出声 | < 500ms |
| 未分离歌曲点击到出声 | < 3s（流式预热） |
| 切换原伴唱响应 | 0 延迟 |
| 搜索响应 | < 100ms |
| 分离质量 | Spleeter 2-stems 基线，伴奏可用于 K 歌 |
| 单曲存储增量 | 约 3-6MB（双 Opus） |
| 模型大小 | ≤ 80MB（首次启动可选「按需下载模型」） |
| 兼容机型 | Android 8.0+，arm64-v8a |

---

## 8. UI 结构（信息架构）

```
首页（Tab Bar）
├─ 我的曲库
│   ├─ 顶部搜索框
│   ├─ 筛选 tab：全部 / 已分离 / 未分离 / 收藏
│   ├─ 排序按钮
│   └─ 歌曲列表（分页加载，每行：封面 + 歌名/歌手 + 状态 badge + 操作按钮）
├─ 点歌队列
│   └─ 队列列表（可拖动排序、置顶、删除）
└─ 设置
    ├─ 导入歌曲（选文件 / 选文件夹 / 输入 URL）
    ├─ 分离任务（队列状态、仅充电时执行开关）
    ├─ 模型管理（下载/删除）
    └─ 关于

播放页（全屏）
├─ 封面 + 歌名/歌手
├─ 滚动歌词
├─ 进度条
├─ 主滑杆：人声音量（核心控件，醒目）
├─ 控制栏：上一首 / 暂停 / 下一首 / 队列入口
└─ 顶部：返回 / 收藏 / 分享（V2）
```

---

## 9. 开发里程碑（MVP 6 周建议）

| 周 | 目标 |
|---|---|
| W1 | 项目搭建、Room 表结构、文件导入 + 元数据解析 + 列表/分页/搜索 |
| W2 | 双轨播放器（先用预先分离好的样例文件验证同步） + 原伴唱滑杆 + 歌词显示 |
| W3-4 | TFLite Spleeter 集成、流式分离 chunk 流水线、缓存与续做 |
| W5 | 队列、收藏、设置、未分离歌曲降级播放、UI 打磨 |
| W6 | 性能调优（NNAPI delegate、低端机降级）、Bug 修复、内测 |

---

## 10. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 低端机分离过慢 | 流式追不上播放 | 检测设备性能，低端机降级为「先全曲分离再播」+ 进度提示 |
| 模型体积大 | APK 膨胀 | 模型不打进 APK，首次启动从你自建的静态 CDN 拉取（~80MB） |
| 电池/发热 | 用户体验差 | 后台分离仅在充电时；前台分离限制在当前播放歌曲 |
| 分离质量不稳定 | 部分歌曲伴奏有人声残留 | 给用户一个「重新分离（高质量模式）」按钮，可选更慢但质量更好的模型 |
| 版权 | 用户上传/分享 | MVP 仅处理本地文件，不做任何上传/分享功能；分离结果也只存本地 |

---

## 11. 未来扩展（V2+）

- 音调升降（SoundTouch）
- 录音 + 与原唱对比
- 简单打分（基于音高匹配）
- 投屏 / 大屏模式
- 歌词手动编辑器
- 高质量模型分级（Spleeter / Demucs 可选）
- 多语言（先简体中文）
